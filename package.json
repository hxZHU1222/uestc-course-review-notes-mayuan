{
  "name": "uestc-course-review-notes-mayuan",
  "version": "1.0.0",
  "private": true,
  "description": "Static web app packaged with Capacitor for mobile release builds.",
  "scripts": {
    "build:web": "node scripts/prepare-capacitor-web.mjs",
    "build:ios": "npm run build:web && node scripts/write-capacitor-config.mjs && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
